<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_slatkishka"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_slatkishka"
        android:supportsRtl="true"
        android:theme="@style/Theme.Slatkishka">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="АPI_kluc" />
        <!--TODO: Тука треба да се вметне API клучот, но ми истече бесплатнниот период за maps! -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>